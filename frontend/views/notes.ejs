<%- include('partials/header', { title: 'Notes - Note-Taking App' }) %>
<main>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-8 col-md-10 col-sm-12">
                <h1 class="text-center mb-4">Your Notes</h1>
                <div class="card shadow mb-4">
                    <div class="card-body">
                        <input type="hidden" id="note-id"> <!-- Hidden field for the note ID -->
                        <div class="mb-3">
                            <label for="title" class="form-label">Title:</label>
                            <input type="text" id="title" class="form-control" required minlength="3" maxlength="100" placeholder="Enter note title">
                            <div id="title-error" class="error-message">Title must be between 3 and 100 characters.</div>
                        </div>
                        <div class="mb-3">
                            <label for="content" class="form-label">Content:</label>
                            <textarea id="content" class="form-control" rows="5" required minlength="10" placeholder="Enter note content"></textarea>
                            <div id="content-error" class="error-message">Content must be at least 10 characters long.</div>
                        </div>
                        <div class="card shadow mb-4">
                            <div class="card-body">
                                <button id="note-add" class="btn btn-primary">Add Note</button>
                                <button id="note-update" class="btn btn-success d-none">Update Note</button> <!-- Update button, initially hidden -->
                                <button id="logout-button" class="btn btn-danger">Logout</button>
                            </div>
                        </div>
                    </div>                    
                </div>
                <div class="card shadow">
                    <div class="card-body">
                     
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Title</th>
                                    <th>Content</th>
                                    <th>Date</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody id="notes-table"  class="scrollable-tbody">
                                <!-- Notes will be added here dynamically -->
                            </tbody>
                        </table>

                        <!-- Pagination controls -->
                        <nav>
                            <ul class="pagination justify-content-center">
                                <li class="page-item disabled" id="prev-page">
                                    <a class="page-link" href="#" tabindex="-1" aria-disabled="true">Previous</a>
                                </li>
                                <li class="page-item active">
                                    <a class="page-link" href="#">1</a>
                                </li>
                                <li class="page-item">
                                    <a class="page-link" href="#">2</a>
                                </li>
                                <li class="page-item">
                                    <a class="page-link" href="#">3</a>
                                </li>
                                <li class="page-item" id="next-page">
                                    <a class="page-link" href="#">Next</a>
                                </li>
                            </ul>
                        </nav>
                      </div>
                   
                </div>
            </div>
        </div>
    </div>
</main>
<script src="/scripts/notes.js"></script>

<%- include('partials/footer') %>